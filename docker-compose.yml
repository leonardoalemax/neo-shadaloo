services:
  neo-shadaloo:
    build: .
    ports:
      - "8080:8080"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      SF6_COOKIE: ${SF6_COOKIE}
      PORT: "8080"
    restart: unless-stopped
